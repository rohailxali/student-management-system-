# Scholarly — internal working notes

## Stack & state (as of Phase 3)
- Project: /home/ubuntu/student-management-system (web-db-user scaffold: tRPC + React 19 + Tailwind 4 + drizzle mysql2 + Vitest)
- Dev URL: https://3000-ifffusqn9vhdjag5smul5-84886500.us1.manus.computer (port 3000, on_change strategy)
- Database: migration applied (0001_daily_luckman.sql). Tables: users (timestamps now INT unix seconds), students, academic_records, activity_log.
- Backend complete: server/db.ts (listStudents w/ search/grade/status/sort/pagination, CRUD, grades, records, activity log, dashboard stats); server/routers.ts (students.list/get/create/update/delete/grades, records.list/create/delete, activity.list, dashboard.stats); shared/schema.ts (zod validation: insertStudentSchema, updateStudentSchema, listStudentsSchema, insertRecordSchema, STUDENT_STATUSES, GRADES).
- Dates in DB: enrollmentDate/dateOfBirth/recordedAt = DATE columns; API sends ISO strings; db.ts converts via toDate().
- Timestamps createdAt/updatedAt/int = unix seconds.

## Design system
- Scandinavian minimalist: pale cool gray bg (oklch 0.965 0.006 240), ink headings, pastel blue primary (oklch 0.55 0.13 255), blush pink accent (oklch 0.93 0.03 20).
- Fonts: Fraunces (display headings) + Inter (body), via client/index.html Google Fonts.
- client/src/index.css: theme vars, .rise-in/.fade-in/.scale-in animations, .card-soft, .geo-shape.
- Layout: client/src/components/ScholarlyLayout.tsx (sidebar with exactly: Dashboard /, Students /students, Settings /settings; NAV_ITEMS exported; sign-in gate).
- client/src/components/shared.tsx: AnimatedNumber, StatusBadge, PageSkeleton, EmptyState, PageHeader, StatCard.

## Routes (App.tsx)
- / Home (Dashboard), /students Students, /students/new StudentForm, /students/:id StudentDetail, /students/:id/edit StudentForm, /settings Settings.

## Progress status (updated)
- DONE: Home.tsx (Dashboard), Students.tsx (directory w/ URL-persisted filters via client/src/hooks/useQueryState.ts), StudentForm.tsx (add/edit 8 fields), StudentDetail.tsx (profile+records+timeline+delete dialog), Settings.tsx (in progress — remaining TS fixes below), App.tsx routes wired, ScholarlyLayout.tsx, shared.tsx components, @shared/schema.ts (SortField/SORT_FIELDS/StudentStatus exported), lib/activity.ts (ACTIVITY_LABELS), index.css theme + animations, index.html fonts, App.tsx ThemeProvider switchable.
- DB: 3 tables exist; API works (tested curl 200 on / and dashboard.stats). No seed data (do NOT fake testimonials — no reviews used).
- Settings.tsx remaining TS fixes: (1) destructure toggleTheme not setTheme from useTheme; (2) remove trpc.system.clearAllData mutation — implement reset via trpc.students.reset (ADD THIS to server/routers.ts if kept) OR remove danger-zone card entirely; simplest: keep card, add router procedure students.reset or keep card removed.
- Also unused ApiError type OK to remove.

## Remaining TODO pages to create
1. client/src/hooks/useQueryState.ts — URL-query-persisted state hook (T, set via wouter setSearchParams). Students.tsx imports it.
2. @shared/db-types — WRONG import in Students.tsx line 26; fix to: import type { SortField } from "@/../server/db"; or better: define shared sort-field type in shared/schema.ts instead and import from "@/../shared/schema" (i.e. "@shared/schema").
3. Students.tsx line ~235: student.enrollmentDate is a Date object (drizzle mysql2 returns Date for date cols) — render with format() from date-fns. Also student.createdAt. Same for Home.tsx monthLabel(student.createdAt) — createdAt is number, monthLabel expects unix sec: OK.
4. StudentForm.tsx (add/edit, fields EXACTLY: name, student ID, email, phone, date of birth, grade/class, enrollment date, status — no more no less).
5. StudentDetail.tsx (profile, academic records, activity timeline, back nav).
6. Settings.tsx (functional: school name stored in localStorage or db; profile info).
7. Vitest tests in server/*.test.ts then pnpm test + pnpm check + lint (eslint likely not installed — check package.json scripts: check=tsc, format=prettier, test=vitest).
8. No checkpoint yet; checkpoint only at delivery end.
9. Deliver final summary (what built, routes, components, schema, API, env vars, run locally, deploy via Publish button).

## UX constraints from user
- Search/filter state preserved across navigation (URL params ✓).
- Empty + loading states everywhere ✓ pattern.
- Mobile responsive ✓ (sidebar mobile top bar).
- Confirm dialog before delete.
